
 .global fib

fib:
	push {lr}

case_zero:
	cmp r0, #0
	bne case_one
	mov r0, #0
	push {r0}
	mov pc, lr

case_one:
	cmp r0, #1
	bne fib_seq
	mov r0, #1
	push {r0}
	mov pc, lr

fib_seq:

	push{r0}

	sub r7, r0, #1
	bl fib
	pop{r0}

	sub r8, r0, #2
	bl fib

	add r9, r8, r7

	pop{lr}
	mov pc, lr
