
 .global fib

fib:
	push {lr}

case_zero:
	cmp r1, #0
	bne case_one
	mov r0, #0
	mov pc, lr

case_one:
	cmp r1, #1
	bne fib_seq
	mov r0, #1
	mov pc, lr

fib_seq:

	push{r0}
	sub r1, r1, #1
	bl fib
	pop{r3}

	sub r1, r1, #2
	bl fib
	pop{r4}

	add r3, r3, r4

	mov pc, lr
